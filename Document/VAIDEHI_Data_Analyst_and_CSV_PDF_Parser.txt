VAIDEHI — Data Analyst & CSV/PDF Parser
Responsible for: Cleaning raw bank statement data, building the parsing pipeline, validating ML output
Tools to use: Python, pandas, numpy, pdfplumber, camelot, Jupyter Notebook
Phase 1 — Data Collection (Days 1–3)
Task 1 — Collect Sample Bank Statements Find 2–3 sample bank statement CSV files. Sources: your own bank's export, Kaggle datasets ("bank transactions dataset"), or create mock data in Excel and export as CSV. Make sure you have at least 100 transaction rows total to train Manan's model properly.
Task 2 — Understand the Formats Open each CSV in Jupyter Notebook. Check: what columns exist? What are they named? What format is the date in? Are amounts negative for expenses? Write your findings in a data_notes.md file and share with the team.
Phase 2 — Build the Parser (Days 4–7)
Task 3 — CSV Cleaning Function Write a Python function called parse_csv(file_path). It must do these steps in order:
Read the CSV file using pandas
Rename columns to standard names: date, description, amount
Remove any rows where description or amount is empty
Convert date column to proper datetime format
Remove duplicate rows
Return a clean pandas DataFrame
Task 4 — PDF Parsing Function Write parse_pdf(file_path) using pdfplumber. Open the PDF, loop through each page, extract any tables found. Merge all pages into one DataFrame. Pass it through the same cleaning steps as Task 3.
Task 5 — Output Contract (Very Important) Your final output must always be a list of JSON objects in exactly this format:
{"date": "2025-06-01", "description": "Zomato Order", "amount": -480}
Expenses must be negative numbers. Income must be positive. Raj's backend depends 100% on this exact format — do not change it without telling him.Phase 3 — Validate & Support (Days 8–16)
Task 6 — Validate Manan's ML Output After Manan trains his model (Day 8), take 50 of your cleaned transactions and manually label what category each one should be (Food, Transport, etc.). Run Manan's model on the same 50. Compare. Calculate accuracy percentage. Write a short report: "Model got X out of 50 correct." Share with Sarthak.
Task 7 — Monthly Summary Functions Write these helper Python functions that Raj will use in the reports feature:
avg_spend_per_category(df) — returns average monthly spend per category
top_3_categories(df) — returns the 3 highest spending categories
month_over_month_change(df) — returns percentage change vs last month
✅ Vaidehi's Final Deliverables:
Clean parser function ready for Raj → Day 7
Sample dataset with 100+ transactions → Day 4
ML accuracy validation report → Day 11
Monthly summary functions → Day 14

